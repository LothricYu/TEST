server:
  port: 6161

spring:
  activemq:
    broker-url:  tcp://localhost:61616
    user:  admin
    password:  admin
  jms:
    pub-sub-domain: false  #false表示队列queue，true表示topic，不写默认队列


#自定义队列和主题

myqueue: boot-activemq-quue